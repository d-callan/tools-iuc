# This file is intentionally left empty.
# Test zip files are no longer needed — the recompute, gene-stats, and combine
# tools now accept individual TSV/FASTA files as inputs and reconstruct the
# directory structure internally.
