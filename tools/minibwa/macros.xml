<macros>
    <token name="@TOOL_VERSION@">0.4</token>
    <token name="@VERSION_SUFFIX@">0</token>
    <token name="@PROFILE@">24.1</token>
    <xml name="bio_tools">
        <xrefs>
            <xref type="bio.tools">minibwa</xref>
        </xrefs>
    </xml>
    <xml name="requirements">
        <requirements>
            <requirement type="package" version="@TOOL_VERSION@">minibwa</requirement>
        </requirements>
    </xml>
    <xml name="version_command">
        <version_command>minibwa version</version_command>
    </xml>
    <xml name="stdio">
        <stdio>
            <exit_code range="1:"/>
            <exit_code range=":-1"/>
            <regex match="ERROR:"/>
            <regex match="\[E::"/>
            <regex match="ABORT!"/>
            <regex match="failed to"/>
        </stdio>
    </xml>
    <token name="@THREADS@">-t \${GALAXY_SLOTS:-1}</token>
    <xml name="citation">
        <citations>
            <citation type="doi">10.48550/arXiv.2606.15357</citation>
            <yield/>
        </citations>
    </xml>
    <token name="@ATTRIBUTION@"><![CDATA[
**Attribution**

This Galaxy tool relies on the minibwa toolkit from `lh3/minibwa
<https://github.com/lh3/minibwa>`_, developed by Heng Li and Nils Homer.
    ]]></token>
    <xml name="reference_source">
        <conditional name="reference_source">
            <param name="reference_source_selector" type="select" label="Choose the source for the reference genome">
                <option value="cached">Use a built-in genome</option>
                <option value="history">Use a genome from the history</option>
            </param>
            <when value="cached">
                <param name="fasta_ref" type="select" label="Reference genome">
                    <options from_data_table="fasta_indexes">
                        <validator type="no_options" message="A built-in reference genome is not available for the build associated with the selected input file"/>
                    </options>
                </param>
            </when>
            <when value="history">
                <param name="fasta_ref" type="data" format="fasta,fasta.gz" label="Reference genome"/>
            </when>
        </conditional>
    </xml>
</macros>
